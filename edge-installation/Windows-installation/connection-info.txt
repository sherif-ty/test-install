# Service Account
USE_SERVICE_ACCOUNT=no             # yes = use custom account, no = use LocalSystem
SERVICE_USERNAME=domain\username   # required if using custom account
SERVICE_PASSWORD=yourpassword      # required if using custom account

# Proxy Settings
USE_PROXY=no                       # yes to enable proxy
PROXY_TYPE=http+socks              # options: none, http, http+socks
HTTP_PROXY_IP=192.168.1.100        # required for http or http+socks
HTTP_PROXY_PORT=8080               # required for http or http+socks
SOCKS_PROXY_IP=192.168.1.101       # required for http+socks
SOCKS_PROXY_PORT=1080              # required for http+socks

# Cribl Leader Info
LEADER_IP=leaderURL  # Leader hostname or IP
LEADER_TOKEN=TOKEn                              # Auth token for registration

# Optional Settings
FLEET_NAME=default_fleet           # fleet name (default: default_fleet)
ENABLE_TLS=yes                     # yes to enable TLS
